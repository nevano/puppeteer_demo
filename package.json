{
  "scripts": {
    "test": "mocha -r babel-register 'test/**/*.test.js'"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "mocha": "^5.1.1",
    "puppeteer": "^1.3.0"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "node": "current"
        }
      ]
    ]
  }
}
